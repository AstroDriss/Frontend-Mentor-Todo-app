const Footer = ({ className }) => {
  return (
    <footer className={`text-darkGrayishBlue text-sm text-center ${className}`}>
      Drag and drop to reorder list
    </footer>
  );
};

export default Footer;
